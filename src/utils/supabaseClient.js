/**
 * Supabase is only used for uploads via Vercel serverless API (/api/upload/image, /api/upload/document).
 * No client-side Storage uploads; this file is kept for reference or future read-only use.
 */
export const BUCKET = 'vehicle-photos'
